import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pierre.bossis.genshin.project',
  appName: 'GenshinIonic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
